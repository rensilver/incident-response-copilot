# Needed: tests/unit/demo/test_scenarios.py already claims that basename under
# pytest's default import mode, and this is the only test subpackage with a
# collision — see this file's own git history for the reproduction.
